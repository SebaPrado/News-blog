<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="../css/styles.css" />
  </head>
  <body>
    <header>
      <%- include('partials/navbar')%>
      <div class="d-flex align-items-center justify-content-center hero-wrapper-img">
        <div class="d-flex flex-column align-items-center">
          <h1>Hack the Blog</h1>
          <h6>El Blog de Hack Academy</h6>
        </div>
      </div>
      <div class="container mt-5 home-container">
        <% for(const article of articles) { %>
        <div class="d-flex flex-column">
          <a class="home-article-links" href="/articulos/<%=article.id%>"><%=article.title%></a>
          <div class="d-flex">
            <small>Creado por <%=article.user.firstname%> <%=article.user.lastname%> </small>
            <small> el <%=article.createdAt%> </small>
          </div>
          <hr />
          <%}%>
        </div>
      </div>
    </header>
    <footer><%- include ('partials/footer') %></footer>
  </body>
</html>
